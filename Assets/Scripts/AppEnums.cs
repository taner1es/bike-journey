namespace AppEnums
{
    public enum DestinationNames { School = 0, Playground = 1, Beach = 2, Camp = 3, Home = 4};
    public enum ApplicationStates { StartMenu, StoryMap, VideoSection, BalloonGame, MatchingGame };
}